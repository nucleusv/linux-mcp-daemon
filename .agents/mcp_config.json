{
  "mcpServers": {
    "linux-mcp-daemon": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/client-sse",
        "--url",
        "http://localhost:9090/sse",
        "--header",
        "Authorization: Bearer my-test-token-123"
      ]
    }
  }
}
