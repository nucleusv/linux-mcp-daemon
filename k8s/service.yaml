apiVersion: v1
kind: Service
metadata:
  name: linux-mcp-daemon-service
spec:
  type: LoadBalancer
  selector:
    app: linux-mcp-daemon
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
