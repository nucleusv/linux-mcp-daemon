# system/get-os-release

Retrieves the Linux distribution information from `/etc/os-release` and the active kernel architecture from `uname -a`.

## Parameters

*(This tool accepts no parameters)*

## Examples

```json
{}
```
