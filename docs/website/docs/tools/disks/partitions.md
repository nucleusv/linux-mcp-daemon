# partitions

**Tool Name**: `disks/partitions`

Retrieves detailed partition tables for a drive (equivalent to fdisk -l). Returns raw text. Must be run as root (privileged: true). Use this to understand the low-level geometry and partition boundaries of a disk.
