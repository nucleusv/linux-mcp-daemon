# cpu/get-load-average

Retrieves the system's 1, 5, and 15-minute load averages. Reads directly from `/proc/loadavg` for a machine-parseable output, falling back to `uptime` if necessary.

## Parameters

*(This tool accepts no parameters)*

## Examples

```json
{}
```
