# Process Introspection

**URI Template**: `process://{pid}/{target}`

Reads process metadata from procfs. Valid targets: status, cmdline, environ. Hint: Find PIDs using the processes/list tool first.
