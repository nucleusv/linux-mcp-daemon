users:
  admin:
    privileged:
      tools:
        kernel/sysctl:
          allowed: true
  unpriviliged:
    privileged:
      tools: {}
      resources: {}
  testuser:
    privileged:
      tools:
        files/list:
          allowed: true
          paths:
            - /
        disks/free:
          allowed: true
        disks/usage:
          allowed: true
        processes/delete:
          allowed: true
        network/connections:
          allowed: true
        network/nslookup:
          allowed: true
        network/curl:
          allowed: true
        network/arp:
          allowed: true
        network/ping:
          allowed: true
        network/trace-path:
          allowed: true
        memory/usage:
          allowed: true
        cpu/list:
          allowed: true
        cpu/load-average:
          allowed: true
        disks/list:
          allowed: true
        disks/performance:
          allowed: true
        disks/health:
          allowed: true
        disks/partitions:
          allowed: true
        system/os-release:
          allowed: true
      resources:
        devices://usb:
          - "*"
        devices://pci:
          - "*"
        devices://dmi:
          - "*"
        kernel://modules:
          - "*"
        network://routes:
          - "*"
        network://interfaces:
          - "*"
        disks://:
          - "*"