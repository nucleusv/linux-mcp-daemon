users:
  admin:
    privileged:
      tools:
        manage_sysctl:
          allowed: true
  testuser:
    privileged:
      tools:
        list_files:
          allowed: true
          paths:
            - /var/log
            - /etc
        get_free:
          allowed: true
        get_usage:
          allowed: true
        delete_process:
          allowed: true
        list_connections:
          allowed: true