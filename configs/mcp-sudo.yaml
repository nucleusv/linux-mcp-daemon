rules:
  - user: "admin"
    tools:
      - name: "manage_sysctl"
        run_as: "root"
  - user: "testuser"
    tools:
      - name: "list_directory"
        run_as: "root"
      - name: "get_disk_space"
        run_as: "root"
      - name: "get_disk_usage"
        run_as: "root"