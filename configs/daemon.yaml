server:
  port: 9090
rate_limits:
  default_rps: 10.0
  default_burst: 20

worker:
  timeout_seconds: 30

tools:
  get_disk_usage:
    timeout_seconds: 300

users:
  - username: "testuser"
    token: "my-test-token-123"